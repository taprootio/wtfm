---
id: WTFM0007
title: Help manifest and anchor drift check
status: completed
completed_on: 2026-07-23
milestone: Help & Docs Surfaces
priority: Medium
effort: 5
depends_on: [WTFM0006]
owner: jereme
summary: Emit a machine-readable help manifest (surface → URLs → anchor list) at build time and ship a check command that fails when a consumer-supplied expected-anchor list no longer matches the built docs — turning silent anchor drift into a CI failure.
---

# WTFM0007 - Help manifest and anchor drift check

Anchor drift is the URL-help system's silent failure mode: a heading id that
stops matching an editor `field-name` breaks that field's help with no error
anywhere (called out in Taproot TR00385). This task makes the contract
checkable by machines on both sides.

## Scope

- **Help manifest output.** On Eleventy's `eleventy.after`, inspect the final
  build results and emit a versioned `help-manifest.json` describing every
  surface (WTFM0005): slug, final path-prefixed reference-page URL,
  path-prefixed help-document URL (WTFM0006), and ordered help-heading ids.
  Missing help pages and duplicate ids fail the build.
- **Check command.** Add an importable checker plus the
  `wtfm-check-help-anchors` CLI. It accepts the built manifest and a versioned
  expected-anchors file exported by a consumer. Missing surfaces/anchors exit
  non-zero with actionable diagnostics. Orphaned extras warn by default and
  fail under `--strict`.
- Document the expected-anchors file format and the intended CI wiring (the
  consumer runs the check after its docs build; Taproot would run it in the
  TR00384 site's workflow).
- Tests: manifest shape from the real fixture build; library and CLI
  pass/fail behavior for matching, missing, duplicate, orphaned, malformed,
  and version-mismatched inputs.

## Notes

- The manifest doubles as a link index for anything that wants to construct
  help URLs programmatically (e.g. the editor wiring in Taproot TR00386)
  instead of hardcoding paths.
- Keep the check read-only and fast — it gates consumer CI, not wtfm's.

## Risks

- Expected-anchor lists living in the consumer can themselves go stale
  against the *editor* — the check closes the docs-side gap; pairing
  field-name changes with anchor updates remains a consumer convention
  (Taproot TR00385 keeps anchors under test beside the field names).
