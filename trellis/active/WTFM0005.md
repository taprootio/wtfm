---
id: WTFM0005
title: Surface pages — one doc page per unit of display
status: active
milestone: Help & Docs Surfaces
priority: High
effort: 8
depends_on: []
owner: jereme
summary: Let components declare membership in a named surface (a unit of display like a page editor or form) and generate one composed doc page per surface — declaration tags, a surfaces data helper for nav and URLs, and rendering that composes member components in order.
---

# WTFM0005 - Surface pages — one doc page per unit of display

wtfm's model today is strictly **one page per component**: `renderDocs(name)`
renders a single CEM declaration, and `data/components.js` builds nav from
per-component `@docUrl` tags. Taproot's Help & Docs Surfaces initiative
(WTFM0003, Taproot TR00382) documents the app as **one page per surface** — a
unit of display such as the page editor or an individual page-type form —
where a surface typically comprises several components (the editor shell plus
its field components). The Espalier flyout links to surface pages.

## Scope

- **Declaration model.** JSDoc tags letting components form surfaces — e.g. a
  defining component carries `@docSurface <slug>` (plus title/menu metadata),
  and member components attach with `@docSurfacePart <slug>` and an order.
  Decide and document the exact tag set; register the tags in the cem-plugin /
  documented `jsDocTagsPlugin` tag set. A component may remain surface-less
  (current behavior untouched).
- **Data helper.** A `data/surfaces.js` export (sibling of
  `data/components.js`): one pagination-ready entry per surface — slug, URL,
  page title, menu metadata, ordered member declarations — for consumer
  templates to build nav and pages from.
- **Rendering.** Compose a surface page from its members' docs in declared
  order (a `renderSurfaceDocs(slug)` shortcode or a documented template
  pattern over the existing `renderDocs`), with per-member section control
  still honoring `@docSections`.
- **Stability.** Surface slugs/URLs are permanent link targets (help links
  point at them) — document that renames are breaking.
- Tests: surface collection from a fixture CEM, member ordering, and composed
  rendering.

## Notes

- Per-component pages keep working unchanged — surfaces are additive.
- Anchor disambiguation across a surface's members is coordinated with
  WTFM0004 (shared slug function).
- Consumed by Taproot TR00384 (site + nav) and TR00385 (help docs per
  surface); the help-document variant of a surface page is WTFM0006.

## Risks

- Authoring-model bikeshed: keep the tag set minimal (slug, title, order) —
  richer grouping can layer on later without breaking the declaration
  contract.
- A surface spanning components from multiple modules relies on CEM analysis
  having captured them all — surface membership should warn loudly when a
  declared part is missing from the manifest rather than silently dropping
  it.
